rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite leitura e escrita para usuários autenticados (inclusive anônimos)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
