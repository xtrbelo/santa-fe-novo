import React from 'react';
import { Card } from '../ui/Card';
import { Button } from '../ui/Button';

export const AccessScreen = ({ icon, iconClass, title, description, notice, user, profile, showAccountDetails = true, onSignOut, onVerify, primaryLabel = 'Verificar liberação', checking, secondaryAction, secondaryLabel, secondaryDisabled }) => <div className="min-h-screen bg-gray-100/70 flex items-center justify-center p-4"><Card className="max-w-lg w-full p-8 text-center space-y-6 shadow-xl !border-none"><div className={iconClass}>{icon}</div><div><h1 className="text-xl font-black text-gray-900">{title}</h1><p className="text-gray-500 text-sm mt-2">{description}</p>{notice && <p className="text-indigo-700 bg-indigo-50 rounded-xl p-3 text-sm font-bold mt-4">{notice}</p>}</div>{showAccountDetails && <div className="bg-gray-50 rounded-xl p-4 text-left text-sm break-all"><p><strong>Nome:</strong> {profile?.nome || user.displayName || 'Não informado'}</p><p><strong>E-mail:</strong> {profile?.email || user.email || 'Não informado'}</p><p><strong>UID:</strong> {user.uid}</p></div>}{secondaryAction && <Button onClick={secondaryAction} disabled={secondaryDisabled} variant="secondary" className="w-full">{secondaryLabel}</Button>}{onVerify && <Button onClick={onVerify} disabled={checking} className="w-full">{checking ? 'Verificando...' : primaryLabel}</Button>}<Button onClick={onSignOut} variant="secondary" className="w-full">Sair</Button></Card></div>;
